package com.micsig.base; // 定义基础工具类包

import android.util.Log; // 导入Android日志类

/**
 * ┌──────────────────────────────────────────────────────────────────────────────┐
 * │                              Logger 日志工具类                               │
 * │                          统一日志管理与调试输出                               │
 * ├──────────────────────────────────────────────────────────────────────────────┤
 * │ 【模块定位】                                                                 │
 * │   MHO示波器基础工具模块 - 日志管理组件                                        │
 * │   提供统一的日志输出接口，支持调试开关控制                                     │
 * ├──────────────────────────────────────────────────────────────────────────────┤
 * │ 【核心职责】                                                                 │
 * │   1. 提供统一的日志输出方法（i/d/e/w/v）                                     │
 * │   2. 支持全局调试开关控制                                                   │
 * │   3. 支持多种参数形式（标签、对象、异常）                                     │
 * │   4. 支持超长日志分块输出                                                   │
 * ├──────────────────────────────────────────────────────────────────────────────┤
 * │ 【架构设计】                                                                 │
 * │   封装Android Log类，提供更便捷的API                                        │
 * │   通过DEBUG开关控制日志输出，生产环境可关闭                                   │
 * │   所有方法均为静态方法，无需实例化                                           │
 * ├──────────────────────────────────────────────────────────────────────────────┤
 * │ 【数据流向】                                                                 │
 * │   日志内容 → DEBUG检查 → Log输出 → Logcat                                  │
 * ├──────────────────────────────────────────────────────────────────────────────┤
 * │ 【依赖关系】                                                                 │
 * │   依赖: android.util.Log (Android日志系统)                                  │
 * │   被依赖: 示波器所有模块的日志输出                                           │
 * ├──────────────────────────────────────────────────────────────────────────────┤
 * │ 【使用示例】                                                                 │
 * │   Logger.DEBUG = true;  // 开启调试日志                                    │
 * │   Logger.i("TAG", "信息日志");                                             │
 * │   Logger.e("TAG", "错误日志", exception);                                  │
 * │   Logger.vLarg("超长日志内容...");                                          │
 * └──────────────────────────────────────────────────────────────────────────────┘
 * 
 * @author Micsig R&D Team
 * @version 1.0
 * @since MHO Series Oscilloscope Software
 */
public class Logger { // 日志工具类
    
    // ==================== 成员变量 ====================
    public static boolean DEBUG; // 全局调试开关，true表示开启日志输出
    static final String DEBUG_TAG = "Logger"; // 默认日志标签

    // ==================== 基础日志方法（仅消息） ====================
    
    /**
     * ┌────────────────────────────────────────────────────────────────────────┐
     * │ 输出INFO级别日志（仅消息）                                              │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【功能说明】                                                            │
     * │   输出INFO级别的日志信息，使用默认标签                                    │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【参数说明】                                                            │
     * │   @param msg 日志消息内容                                               │
     * └────────────────────────────────────────────────────────────────────────┘
     */
    public static void i(String msg) { // INFO日志方法
        if (DEBUG) { // 检查调试开关
            Log.i(DEBUG_TAG, msg + ""); // 输出日志，附加空字符串避免null
        }
    }

    /**
     * ┌────────────────────────────────────────────────────────────────────────┐
     * │ 输出DEBUG级别日志（仅消息）                                             │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【功能说明】                                                            │
     * │   输出DEBUG级别的日志信息，使用默认标签                                    │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【参数说明】                                                            │
     * │   @param msg 日志消息内容                                               │
     * └────────────────────────────────────────────────────────────────────────┘
     */
    public static void d(String msg) { // DEBUG日志方法
        if (DEBUG) { // 检查调试开关
            Log.d(DEBUG_TAG, msg + ""); // 输出日志
        }
    }

    /**
     * ┌────────────────────────────────────────────────────────────────────────┐
     * │ 输出ERROR级别日志（仅消息）                                             │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【功能说明】                                                            │
     * │   输出ERROR级别的日志信息，使用默认标签                                    │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【参数说明】                                                            │
     * │   @param msg 日志消息内容                                               │
     * └────────────────────────────────────────────────────────────────────────┘
     */
    public static void e(String msg) { // ERROR日志方法
        if (DEBUG) { // 检查调试开关
            Log.e(DEBUG_TAG, msg + ""); // 输出日志
        }
    }

    /**
     * ┌────────────────────────────────────────────────────────────────────────┐
     * │ 输出WARN级别日志（仅消息）                                              │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【功能说明】                                                            │
     * │   输出WARN级别的日志信息，使用默认标签                                     │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【参数说明】                                                            │
     * │   @param msg 日志消息内容                                               │
     * └────────────────────────────────────────────────────────────────────────┘
     */
    public static void w(String msg) { // WARN日志方法
        if (DEBUG) { // 检查调试开关
            Log.w(DEBUG_TAG, msg + ""); // 输出日志
        }
    }

    /**
     * ┌────────────────────────────────────────────────────────────────────────┐
     * │ 输出VERBOSE级别日志（仅消息）                                           │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【功能说明】                                                            │
     * │   输出VERBOSE级别的日志信息，使用默认标签                                  │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【参数说明】                                                            │
     * │   @param msg 日志消息内容                                               │
     * └────────────────────────────────────────────────────────────────────────┘
     */
    public static void v(String msg) { // VERBOSE日志方法
        if (DEBUG) { // 检查调试开关
            Log.v(DEBUG_TAG, msg + ""); // 输出日志
        }
    }

    // ==================== 带标签的日志方法 ====================

    /**
     * ┌────────────────────────────────────────────────────────────────────────┐
     * │ 输出INFO级别日志（带标签）                                              │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【功能说明】                                                            │
     * │   输出INFO级别的日志信息，使用自定义标签                                   │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【参数说明】                                                            │
     * │   @param tag 日志标签                                                  │
     * │   @param msg 日志消息内容                                               │
     * └────────────────────────────────────────────────────────────────────────┘
     */
    public static void i(String tag, String msg) { // INFO日志方法（带标签）
        if (DEBUG){ // 检查调试开关
            Log.i(tag, msg); // 输出日志
        }
    }

    /**
     * ┌────────────────────────────────────────────────────────────────────────┐
     * │ 输出DEBUG级别日志（带标签）                                             │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【功能说明】                                                            │
     * │   输出DEBUG级别的日志信息，使用自定义标签                                  │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【参数说明】                                                            │
     * │   @param tag 日志标签                                                  │
     * │   @param msg 日志消息内容                                               │
     * └────────────────────────────────────────────────────────────────────────┘
     */
    public static void d(String tag, String msg) { // DEBUG日志方法（带标签）
        if (DEBUG){ // 检查调试开关
            Log.d(tag, msg); // 输出日志
        }
    }

    /**
     * ┌────────────────────────────────────────────────────────────────────────┐
     * │ 输出ERROR级别日志（带标签）                                             │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【功能说明】                                                            │
     * │   输出ERROR级别的日志信息，使用自定义标签                                  │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【参数说明】                                                            │
     * │   @param tag 日志标签                                                  │
     * │   @param msg 日志消息内容                                               │
     * └────────────────────────────────────────────────────────────────────────┘
     */
    public static void e(String tag, String msg) { // ERROR日志方法（带标签）
        if (DEBUG){ // 检查调试开关
            Log.e(tag, msg); // 输出日志
        }
    }

    /**
     * ┌────────────────────────────────────────────────────────────────────────┐
     * │ 输出WARN级别日志（带标签）                                              │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【功能说明】                                                            │
     * │   输出WARN级别的日志信息，使用自定义标签                                   │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【参数说明】                                                            │
     * │   @param tag 日志标签                                                  │
     * │   @param msg 日志消息内容                                               │
     * └────────────────────────────────────────────────────────────────────────┘
     */
    public static void w(String tag, String msg) { // WARN日志方法（带标签）
        if(DEBUG){ // 检查调试开关
            Log.w(tag, msg); // 输出日志
        }
    }

    /**
     * ┌────────────────────────────────────────────────────────────────────────┐
     * │ 输出VERBOSE级别日志（带标签）                                           │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【功能说明】                                                            │
     * │   输出VERBOSE级别的日志信息，使用自定义标签                                │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【参数说明】                                                            │
     * │   @param tag 日志标签                                                  │
     * │   @param msg 日志消息内容                                               │
     * └────────────────────────────────────────────────────────────────────────┘
     */
    public static void v(String tag, String msg) { // VERBOSE日志方法（带标签）
        if (DEBUG){ // 检查调试开关
            Log.v(tag, msg); // 输出日志
        }
    }

    // ==================== 带对象标签的日志方法 ====================

    /**
     * ┌────────────────────────────────────────────────────────────────────────┐
     * │ 输出INFO级别日志（使用对象类名作为标签）                                 │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【功能说明】                                                            │
     * │   输出INFO级别日志，自动使用对象的类名作为标签                             │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【参数说明】                                                            │
     * │   @param o   对象实例，用于获取类名                                      │
     * │   @param msg 日志消息内容                                               │
     * └────────────────────────────────────────────────────────────────────────┘
     */
    public static void i(Object o, String msg) { // INFO日志方法（对象标签）
        i(o.getClass().getSimpleName() + ":" + msg); // 使用类名作为标签
    }

    /**
     * ┌────────────────────────────────────────────────────────────────────────┐
     * │ 输出DEBUG级别日志（使用对象类名作为标签）                                │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【功能说明】                                                            │
     * │   输出DEBUG级别日志，自动使用对象的类名作为标签                            │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【参数说明】                                                            │
     * │   @param o   对象实例，用于获取类名                                      │
     * │   @param msg 日志消息内容                                               │
     * └────────────────────────────────────────────────────────────────────────┘
     */
    public static void d(Object o, String msg) { // DEBUG日志方法（对象标签）
        d(o.getClass().getSimpleName() + ":" + msg); // 使用类名作为标签
    }

    /**
     * ┌────────────────────────────────────────────────────────────────────────┐
     * │ 输出ERROR级别日志（使用对象类名作为标签）                                │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【功能说明】                                                            │
     * │   输出ERROR级别日志，自动使用对象的类名作为标签                            │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【参数说明】                                                            │
     * │   @param o   对象实例，用于获取类名                                      │
     * │   @param msg 日志消息内容                                               │
     * └────────────────────────────────────────────────────────────────────────┘
     */
    public static void e(Object o, String msg) { // ERROR日志方法（对象标签）
        e(o.getClass().getSimpleName() + ":" + msg); // 使用类名作为标签
    }

    /**
     * ┌────────────────────────────────────────────────────────────────────────┐
     * │ 输出WARN级别日志（使用对象类名作为标签）                                 │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【功能说明】                                                            │
     * │   输出WARN级别日志，自动使用对象的类名作为标签                             │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【参数说明】                                                            │
     * │   @param o   对象实例，用于获取类名                                      │
     * │   @param msg 日志消息内容                                               │
     * └────────────────────────────────────────────────────────────────────────┘
     */
    public static void w(Object o, String msg) { // WARN日志方法（对象标签）
        w(o.getClass().getSimpleName() + ":" + msg); // 使用类名作为标签
    }

    /**
     * ┌────────────────────────────────────────────────────────────────────────┐
     * │ 输出VERBOSE级别日志（使用对象类名作为标签）                              │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【功能说明】                                                            │
     * │   输出VERBOSE级别日志，自动使用对象的类名作为标签                          │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【参数说明】                                                            │
     * │   @param o   对象实例，用于获取类名                                      │
     * │   @param msg 日志消息内容                                               │
     * └────────────────────────────────────────────────────────────────────────┘
     */
    public static void v(Object o, String msg) { // VERBOSE日志方法（对象标签）
        v(o.getClass().getSimpleName() + ":" + msg); // 使用类名作为标签
    }

    // ==================== 带异常的日志方法 ====================

    /**
     * ┌────────────────────────────────────────────────────────────────────────┐
     * │ 输出INFO级别日志（带异常堆栈）                                          │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【功能说明】                                                            │
     * │   输出INFO级别日志，包含异常堆栈信息                                      │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【参数说明】                                                            │
     * │   @param tag  日志标签                                                 │
     * │   @param info 日志消息内容                                              │
     * │   @param e    异常对象                                                  │
     * └────────────────────────────────────────────────────────────────────────┘
     */
    public static void i(String tag, String info, Throwable e) { // INFO日志方法（带异常）
        i(tag + ":" + info + ":" + Log.getStackTraceString(e)); // 拼接标签、消息和堆栈信息
    }

    /**
     * ┌────────────────────────────────────────────────────────────────────────┐
     * │ 输出DEBUG级别日志（带异常堆栈）                                         │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【功能说明】                                                            │
     * │   输出DEBUG级别日志，包含异常堆栈信息                                     │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【参数说明】                                                            │
     * │   @param tag  日志标签                                                 │
     * │   @param info 日志消息内容                                              │
     * │   @param e    异常对象                                                  │
     * └────────────────────────────────────────────────────────────────────────┘
     */
    public static void d(String tag, String info, Throwable e) { // DEBUG日志方法（带异常）
        d(tag + ":" + info + ":" + Log.getStackTraceString(e)); // 拼接标签、消息和堆栈信息
    }

    /**
     * ┌────────────────────────────────────────────────────────────────────────┐
     * │ 输出ERROR级别日志（带异常堆栈）                                         │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【功能说明】                                                            │
     * │   输出ERROR级别日志，包含异常堆栈信息                                     │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【参数说明】                                                            │
     * │   @param tag  日志标签                                                 │
     * │   @param info 日志消息内容                                              │
     * │   @param e    异常对象                                                  │
     * └────────────────────────────────────────────────────────────────────────┘
     */
    public static void e(String tag, String info, Throwable e) { // ERROR日志方法（带异常）
        e(tag + ":" + info + ":" + Log.getStackTraceString(e)); // 拼接标签、消息和堆栈信息
    }

    /**
     * ┌────────────────────────────────────────────────────────────────────────┐
     * │ 输出VERBOSE级别日志（带异常堆栈）                                       │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【功能说明】                                                            │
     * │   输出VERBOSE级别日志，包含异常堆栈信息                                   │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【参数说明】                                                            │
     * │   @param tag  日志标签                                                 │
     * │   @param info 日志消息内容                                              │
     * │   @param e    异常对象                                                  │
     * └────────────────────────────────────────────────────────────────────────┘
     */
    public static void v(String tag, String info, Throwable e) { // VERBOSE日志方法（带异常）
        v(tag + ":" + info + ":" + Log.getStackTraceString(e)); // 拼接标签、消息和堆栈信息
    }

    /**
     * ┌────────────────────────────────────────────────────────────────────────┐
     * │ 输出WARN级别日志（带异常堆栈）                                          │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【功能说明】                                                            │
     * │   输出WARN级别日志，包含异常堆栈信息                                      │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【参数说明】                                                            │
     * │   @param tag  日志标签                                                 │
     * │   @param info 日志消息内容                                              │
     * │   @param e    异常对象                                                  │
     * └────────────────────────────────────────────────────────────────────────┘
     */
    public static void w(String tag, String info, Throwable e) { // WARN日志方法（带异常）
        w(tag + ":" + info + ":" + Log.getStackTraceString(e)); // 拼接标签、消息和堆栈信息
    }

    // ==================== 仅异常的日志方法 ====================

    /**
     * ┌────────────────────────────────────────────────────────────────────────┐
     * │ 输出INFO级别日志（仅异常堆栈）                                          │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【功能说明】                                                            │
     * │   输出INFO级别日志，仅包含异常堆栈信息                                    │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【参数说明】                                                            │
     * │   @param tag 日志标签                                                  │
     * │   @param e   异常对象                                                  │
     * └────────────────────────────────────────────────────────────────────────┘
     */
    public static void i(String tag, Throwable e) { // INFO日志方法（仅异常）
        i(tag + ":" + Log.getStackTraceString(e)); // 拼接标签和堆栈信息
    }

    /**
     * ┌────────────────────────────────────────────────────────────────────────┐
     * │ 输出DEBUG级别日志（仅异常堆栈）                                         │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【功能说明】                                                            │
     * │   输出DEBUG级别日志，仅包含异常堆栈信息                                   │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【参数说明】                                                            │
     * │   @param tag 日志标签                                                  │
     * │   @param e   异常对象                                                  │
     * └────────────────────────────────────────────────────────────────────────┘
     */
    public static void d(String tag, Throwable e) { // DEBUG日志方法（仅异常）
        d(tag + ":" + Log.getStackTraceString(e)); // 拼接标签和堆栈信息
    }

    /**
     * ┌────────────────────────────────────────────────────────────────────────┐
     * │ 输出ERROR级别日志（仅异常堆栈）                                         │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【功能说明】                                                            │
     * │   输出ERROR级别日志，仅包含异常堆栈信息                                   │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【参数说明】                                                            │
     * │   @param tag 日志标签                                                  │
     * │   @param e   异常对象                                                  │
     * └────────────────────────────────────────────────────────────────────────┘
     */
    public static void e(String tag, Throwable e) { // ERROR日志方法（仅异常）
        e(tag + ":" + Log.getStackTraceString(e)); // 拼接标签和堆栈信息
    }

    /**
     * ┌────────────────────────────────────────────────────────────────────────┐
     * │ 输出VERBOSE级别日志（仅异常堆栈）                                       │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【功能说明】                                                            │
     * │   输出VERBOSE级别日志，仅包含异常堆栈信息                                 │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【参数说明】                                                            │
     * │   @param tag 日志标签                                                  │
     * │   @param e   异常对象                                                  │
     * └────────────────────────────────────────────────────────────────────────┘
     */
    public static void v(String tag, Throwable e) { // VERBOSE日志方法（仅异常）
        v(tag + ":" + Log.getStackTraceString(e)); // 拼接标签和堆栈信息
    }

    /**
     * ┌────────────────────────────────────────────────────────────────────────┐
     * │ 输出WARN级别日志（仅异常堆栈）                                          │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【功能说明】                                                            │
     * │   输出WARN级别日志，仅包含异常堆栈信息                                    │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【参数说明】                                                            │
     * │   @param tag 日志标签                                                  │
     * │   @param e   异常对象                                                  │
     * └────────────────────────────────────────────────────────────────────────┘
     */
    public static void w(String tag, Throwable e) { // WARN日志方法（仅异常）
        w(tag + ":" + Log.getStackTraceString(e)); // 拼接标签和堆栈信息
    }

    // ==================== 超长日志方法 ====================

    /**
     * ┌────────────────────────────────────────────────────────────────────────┐
     * │ 输出超长日志（自动分块）                                                │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【功能说明】                                                            │
     * │   输出超长日志内容，自动分块处理                                          │
     * │   Android Logcat单条日志限制约4000字符，此方法自动分割                    │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【参数说明】                                                            │
     * │   @param sb 超长日志内容（StringBuilder）                                │
     * ├────────────────────────────────────────────────────────────────────────┤
     * │ 【算法说明】                                                            │
     * │   1. 如果长度<=4000，直接输出                                           │
     * │   2. 如果长度>4000，按4000字符分块输出                                   │
     * │   3. 每块标注块序号，便于识别                                           │
     * └────────────────────────────────────────────────────────────────────────┘
     */
    public static void vLarg(String sb) { // 超长日志输出方法
        if (sb.length() > 4000) { // 检查是否超过单条日志限制
            v("log.length = " + sb.length()); // 输出总长度信息
            int chunkCount = sb.length() / 4000; // 计算分块数量（整数除法）
            for (int i = 0; i <= chunkCount; i++) { // 循环输出每个分块
                int max = 4000 * (i + 1); // 计算当前块的结束位置
                if (max >= sb.length()) { // 检查是否为最后一块
                    v("chunk " + i + " of " + chunkCount + ":" + sb.substring(4000 * i)); // 输出最后一块
                } else { // 不是最后一块
                    v("chunk " + i + " of " + chunkCount + ":" + sb.substring(4000 * i, max)); // 输出当前块
                }
            }
        } else { // 未超过限制
            v(sb.toString()); // 直接输出
        }
    }

//    public static void errDebug(){
//        StackTraceElement[] stackTraceElements = Thread.currentThread().getStackTrace();
//        e("micsig","TODO:需要支持8通道,数学8通道,参考8通道,总线4通道"
//                + ",Name:" + stackTraceElements[3].getFileName()
//                + "," + stackTraceElements[3].getLineNumber()
//        );
//    }
}
