package com.micsig.tbook.tbookscope.wavezone.wave; // 波形显示区域-波形子模块包，包含示波器波形显示的核心组件，1

import com.chillingvan.canvasgl.ICanvasGL; // OpenGL画布接口，用于OpenGL渲染绘制，1

/**
 * 波形管理接口 - 示波器波形管理功能的核心抽象层
 * 
 * 【模块定位】
 * - 所属模块：wavezone.wave（波形显示区域-波形子模块）
 * - 核心职责：定义波形管理的基本操作契约，为不同工作模式提供统一的波形管理接口
 * - 架构层级：接口层组件，定义波形管理的抽象行为
 * 
 * 【核心职责】
 * 1. 波形绘制管理：定义波形在画布上的绘制方法
 * 2. 通道可见性管理：定义通道显示/隐藏的控制方法
 * 3. 通道位置管理：定义通道垂直位置的设置和获取方法
 * 4. 通道选中管理：定义通道选中状态的管理方法
 * 5. 光标选择管理：定义光标选择和移动的方法
 * 6. 当前通道获取：定义获取当前选中通道的方法
 * 
 * 【架构图】
 * ┌─────────────────────────────────────────────────────────────┐
 * │              WaveZoneDisplay（波形显示核心组件）             │
 * │                   (波形显示核心)                             │
 * │  - 管理波形显示区域                                          │
 * │  - 协调波形绘制和交互                                        │
 * │  - 处理用户操作事件                                          │
 * └─────────────────────────────────────────────────────────────┘
 *                              ↓ 依赖接口
 * ┌─────────────────────────────────────────────────────────────┐
 * │              IWaveManage（波形管理接口）                      │
 * │                (波形管理抽象层)                              │
 * │  ┌──────────────────────────────────────────────────────┐  │
 * │  │  核心方法定义                                        │  │
 * │  │  - draw(): 绘制波形                                  │  │
 * │  │  - setVisible(): 设置通道可见性                      │  │
 * │  │  - setOffsetY(): 设置垂直偏移                        │  │
 * │  │  - setPositionY(): 设置通道垂直位置                  │  │
 * │  │  - getPositionY(): 获取通道垂直位置                  │  │
 * │  │  - setCenterChY(): 设置通道居中                      │  │
 * │  │  - setSelectCursor(): 设置选中光标                   │  │
 * │  │  - selectCursor(): 选择光标                          │  │
 * │  │  - movePix(): 移动像素                               │  │
 * │  │  - getCurCh(): 获取当前通道                          │  │
 * │  └──────────────────────────────────────────────────────┘  │
 * └─────────────────────────────────────────────────────────────┘
 *                              ↑ 实现接口
 * ┌─────────────────────────────────────────────────────────────┐
 * │          ChannelWaveManage_XY（XY模式通道波形管理类）         │
 * │              WaveManage_XY（XY模式波形管理类）                │
 * │                (波形管理实现类)                              │
 * │  - 实现波形管理接口                                          │
 * │  - 管理通道波形对象                                          │
 * │  - 处理波形交互逻辑                                          │
 * └─────────────────────────────────────────────────────────────┘
 * 
 * 【接口方法说明】
 * ┌─────────────────────────────────────────────────────────────┐
 * │ 方法名称          │ 功能说明              │ 调用时机         │
 * ├─────────────────────────────────────────────────────────────┤
 * │ draw()            │ 绘制波形              │ 每帧渲染时       │
 * │ setVisible()      │ 设置通道可见性        │ 用户切换显示时   │
 * │ setOffsetY()      │ 设置垂直偏移          │ 波形区域滚动时   │
 * │ setPositionY()    │ 设置通道垂直位置      │ 用户调整位置时   │
 * │ getPositionY()    │ 获取通道垂直位置      │ 位置同步时       │
 * │ setCenterChY()    │ 设置通道居中          │ 用户点击居中时   │
 * │ setSelectCursor() │ 设置选中光标          │ 外部选中通道时   │
 * │ selectCursor()    │ 选择光标              │ 用户点击选择时   │
 * │ movePix()         │ 移动像素              │ 用户拖动波形时   │
 * │ getCurCh()        │ 获取当前通道          │ 查询选中状态时   │
 * └─────────────────────────────────────────────────────────────┘
 * 
 * 【依赖关系】
 * - 依赖：ICanvasGL（OpenGL画布接口，用于绘制波形）
 * - 被依赖：ChannelWaveManage_XY、WaveManage_XY、WaveManage_YT、WaveManage
 * - 协作：IWorkMode（工作模式接口，部分实现类同时实现此接口）
 * 
 * 【设计模式说明】
 * 1. 接口隔离原则：定义最小化的波形管理接口，避免接口污染
 * 2. 依赖倒置原则：高层模块依赖抽象接口，不依赖具体实现
 * 3. 策略模式：不同工作模式的波形管理实现可互换使用
 * 4. 桥接模式：将波形管理的抽象部分与实现部分分离
 * 
 * 【使用场景】
 * 1. XY模式波形管理：ChannelWaveManage_XY实现，管理XY模式下的双通道波形
 * 2. YT模式波形管理：WaveManage_YT实现，管理YT模式下的多通道波形
 * 3. 通用波形管理：WaveManage实现，管理常规模式下的波形
 * 4. 波形显示组件：WaveZoneDisplay通过此接口管理波形对象
 * 5. 用户交互处理：处理用户的波形选择、移动、位置调整等操作
 * 
 * 【实现类说明】
 * - ChannelWaveManage_XY：XY模式通道波形管理类，管理Ch1（垂直波形）和Ch2（水平波形）
 * - WaveManage_XY：XY模式波形管理类，封装ChannelWaveManage_XY
 * - WaveManage_YT：YT模式波形管理类，管理YT模式下的多通道波形
 * - WaveManage：通用波形管理类，管理常规模式下的波形
 * 
 * 【调用流程示例】
 * 1. 波形绘制流程：
 *    WaveZoneDisplay.draw() → IWaveManage.draw() → 具体实现类绘制波形
 * 
 * 2. 通道位置调整流程：
 *    用户拖动波形 → IWaveManage.movePix() → 更新位置 → 触发重绘
 * 
 * 3. 通道选中流程：
 *    用户点击波形 → IWaveManage.selectCursor() → 设置选中状态 → 触发选中事件
 * 
 * @author liwb
 * @version 1.0
 * @since 2017/11/6
 * @see ChannelWaveManage_XY XY模式通道波形管理实现类
 * @see WaveManage_XY XY模式波形管理实现类
 * @see WaveManage_YT YT模式波形管理实现类
 * @see WaveManage 通用波形管理实现类
 * @see ICanvasGL OpenGL画布接口
 */

public interface IWaveManage {
    
    /**
     * 绘制波形到画布上
     * 
     * 【功能说明】
     * 将波形绘制到指定的OpenGL画布上，由具体实现类决定绘制逻辑。
     * 在XY模式下，会绘制Ch1垂直波形和Ch2水平波形；
     * 在YT模式下，会绘制多个通道的波形。
     * 
     * 【参数说明】
     * @param canvas OpenGL画布对象，用于绘制波形图形
     *              - 类型：ICanvasGL（OpenGL画布接口）
     *              - 用途：提供OpenGL渲染环境，支持高性能图形绘制
     *              - 生命周期：由调用方管理，方法内不负责创建和销毁
     * 
     * 【返回值】
     * 无返回值（void）
     * 
     * 【调用时机】
     * 1. 每帧渲染时调用，由WaveZoneDisplay触发
     * 2. 波形数据更新后需要重绘时调用
     * 3. 用户交互导致波形变化后调用
     * 
     * 【实现要求】
     * 1. 实现类必须保证线程安全，避免并发绘制问题
     * 2. 绘制前应检查通道可见性，只绘制可见通道
     * 3. 绘制顺序应符合业务逻辑（如XY模式先绘制垂直波形再绘制水平波形）
     * 
     * 【异常处理】
     * - canvas为null时，实现类应进行空指针检查或抛出NullPointerException
     * - 绘制失败时，实现类应记录错误日志但不影响程序稳定性
     * 
     * 【性能考虑】
     * - 此方法调用频率高（每帧），应避免在方法内创建对象
     * - 应使用缓存机制优化绘制性能
     * - 复杂绘制逻辑应考虑使用双缓冲技术
     * 
     * 【使用示例】
     * <pre>
     * IWaveManage waveManage = new ChannelWaveManage_XY(...);
     * ICanvasGL canvas = ...; // 获取OpenGL画布
     * waveManage.draw(canvas); // 绘制波形
     * </pre>
     */
    public void draw(ICanvasGL canvas);
    
    /**
     * 设置通道的可见性
     * 
     * 【功能说明】
     * 控制指定通道的显示或隐藏状态。当通道设置为不可见时，
     * 该通道的波形将不会被绘制到画布上。
     * 
     * 【参数说明】
     * @param ChNo 通道编号，标识要设置可见性的通道
     *             - 取值范围：通常为1-9，具体取决于示波器型号
     *             - Ch1：通道1，通常用于主信号输入
     *             - Ch2：通道2，通常用于辅助信号输入
     *             - 其他通道：根据设备支持的通道数量而定
     * @param visible 可见性标志
     *                - true：显示该通道波形
     *                - false：隐藏该通道波形
     * 
     * 【返回值】
     * 无返回值（void）
     * 
     * 【调用时机】
     * 1. 用户点击通道开关按钮时调用
     * 2. 系统初始化时设置默认可见性
     * 3. 工作模式切换时调整通道可见性
     * 4. 外部设备连接/断开时自动调整可见性
     * 
     * 【实现要求】
     * 1. 实现类应维护通道可见性状态
     * 2. 设置不可见后，draw()方法不应绘制该通道
     * 3. 应触发重绘操作以更新显示
     * 4. 可见性变化应通知相关监听器（如有）
     * 
     * 【异常处理】
     * - ChNo超出有效范围时，实现类应忽略该操作或抛出IllegalArgumentException
     * - 通道不存在时，实现类应忽略该操作
     * 
     * 【使用示例】
     * <pre>
     * IWaveManage waveManage = ...;
     * waveManage.setVisible(1, true);  // 显示通道1
     * waveManage.setVisible(2, false); // 隐藏通道2
     * </pre>
     */
    public void setVisible(int ChNo, boolean visible);
    
    /**
     * 设置波形的垂直偏移量
     * 
     * 【功能说明】
     * 设置波形显示区域的垂直偏移量，用于实现波形区域的滚动效果。
     * 该偏移量影响所有通道波形的垂直位置基准。
     * 
     * 【参数说明】
     * @param offsetY 垂直偏移量（像素）
     *                - 单位：像素
     *                - 正值：波形向下偏移
     *                - 负值：波形向上偏移
     *                - 0：无偏移，波形在默认位置
     * 
     * 【返回值】
     * 无返回值（void）
     * 
     * 【调用时机】
     * 1. 用户滚动波形显示区域时调用
     * 2. 系统自动调整显示区域时调用
     * 3. 波形区域大小改变时重新计算偏移量
     * 
     * 【实现要求】
     * 1. 实现类应维护偏移量状态
     * 2. 设置偏移量后应触发重绘操作
     * 3. 应考虑偏移量的边界限制，避免超出显示区域
     * 4. 偏移量应与通道位置分离计算
     * 
     * 【使用场景】
     * - YT模式下波形区域滚动
     * - 多通道波形整体移动
     * - 波形显示区域调整
     * 
     * 【使用示例】
     * <pre>
     * IWaveManage waveManage = ...;
     * waveManage.setOffsetY(50);  // 波形向下偏移50像素
     * waveManage.setOffsetY(-30); // 波形向上偏移30像素
     * </pre>
     */
    public void setOffsetY(int offsetY);
    
    /**
     * 设置指定通道的垂直位置
     * 
     * 【功能说明】
     * 设置指定通道波形在显示区域中的垂直位置。
     * 该位置是相对于显示区域中心的位置，以比例值表示。
     * 
     * 【参数说明】
     * @param chNo 通道编号，标识要设置位置的通道
     *             - 取值范围：1-9（根据注释）
     *             - Ch1：通道1
     *             - Ch2：通道2
     *             - 其他通道根据设备支持而定
     * @param positionY 通道垂直位置（比例值）
     *                 - 单位：比例值（相对于显示区域高度）
     *                 - 取值范围：通常为0.0-1.0，0.5表示居中
     *                 - 0.0：波形在显示区域顶部
     *                 - 0.5：波形在显示区域中心
     *                 - 1.0：波形在显示区域底部
     *                 - 实际范围可能超出0.0-1.0，取决于具体实现
     * 
     * 【返回值】
     * 无返回值（void）
     * 
     * 【调用时机】
     * 1. 用户拖动通道波形调整位置时调用
     * 2. 系统初始化时设置默认位置
     * 3. 从缓存恢复通道位置时调用
     * 4. 工作模式切换时恢复位置设置
     * 
     * 【实现要求】
     * 1. 实现类应维护每个通道的位置状态
     * 2. 设置位置后应触发重绘操作
     * 3. 应考虑位置的边界限制
     * 4. 位置变化应通知相关监听器（如有）
     * 5. 应将位置保存到缓存（如需要持久化）
     * 
     * 【XY模式特殊说明】
     * - Ch1控制X轴位置（水平位置）
     * - Ch2控制Y轴位置（垂直位置）
     * 
     * 【使用示例】
     * <pre>
     * IWaveManage waveManage = ...;
     * waveManage.setPositionY(1, 0.5);  // 设置通道1居中
     * waveManage.setPositionY(2, 0.3); // 设置通道2偏上
     * </pre>
     */
    public void setPositionY(int chNo, double positionY);
    
    /**
     * 获取指定通道的垂直位置
     * 
     * 【功能说明】
     * 获取指定通道波形在显示区域中的垂直位置。
     * 该位置是相对于显示区域中心的位置，以比例值表示。
     * 
     * 【参数说明】
     * @param chNo 通道编号，标识要获取位置的通道
     *             - 取值范围：1-9（根据注释）
     *             - Ch1：通道1
     *             - Ch2：通道2
     *             - 其他通道根据设备支持而定
     * 
     * 【返回值】
     * @return 通道垂直位置（比例值）
     *         - 单位：比例值（相对于显示区域高度）
     *         - 取值范围：通常为0.0-1.0，0.5表示居中
     *         - 0.0：波形在显示区域顶部
     *         - 0.5：波形在显示区域中心
     *         - 1.0：波形在显示区域底部
     *         - 实际范围可能超出0.0-1.0，取决于具体实现
     * 
     * 【调用时机】
     * 1. 需要同步通道位置到其他组件时调用
     * 2. 保存通道位置到缓存时调用
     * 3. 显示通道位置信息时调用
     * 4. 工作模式切换时保存当前位置
     * 
     * 【实现要求】
     * 1. 实现类应维护每个通道的位置状态
     * 2. 通道不存在时应返回默认值（如0.5）
     * 3. 应保证返回值的有效性
     * 
     * 【使用示例】
     * <pre>
     * IWaveManage waveManage = ...;
     * double position = waveManage.getPositionY(1); // 获取通道1的位置
     * if (position != 0.5) {
     *     // 位置不是居中，进行相应处理
     * }
     * </pre>
     */
    public double getPositionY(int chNo);
    
    /**
     * 设置指定通道的垂直居中位置
     * 
     * 【功能说明】
     * 将指定通道的波形垂直位置设置为显示区域的中心位置。
     * 这是一个便捷方法，相当于调用setPositionY(chNo, 0.5)。
     * 
     * 【参数说明】
     * @param chNo 通道编号，标识要居中的通道
     *             - 取值范围：1-9（根据注释）
     *             - Ch1：通道1
     *             - Ch2：通道2
     *             - 其他通道根据设备支持而定
     * 
     * 【返回值】
     * 无返回值（void）
     * 
     * 【调用时机】
     * 1. 用户点击通道居中按钮时调用
     * 2. 系统初始化时设置默认居中位置
     * 3. 重置通道位置时调用
     * 4. 工作模式切换时重置位置
     * 
     * 【实现要求】
     * 1. 实现类应将通道位置设置为0.5（居中）
     * 2. 设置位置后应触发重绘操作
     * 3. 位置变化应通知相关监听器（如有）
     * 4. 应将位置保存到缓存（如需要持久化）
     * 
     * 【XY模式特殊说明】
     * - Ch1居中：X轴位置居中
     * - Ch2居中：Y轴位置居中
     * 
     * 【使用示例】
     * <pre>
     * IWaveManage waveManage = ...;
     * waveManage.setCenterChY(1); // 将通道1居中
     * waveManage.setCenterChY(2); // 将通道2居中
     * </pre>
     */
    public void setCenterChY(int chNo);
    
    /**
     * 设置选中的通道光标
     * 
     * 【功能说明】
     * 设置指定通道为选中状态，并显示选中光标。
     * 通常同一时间只能有一个通道被选中（互斥选择）。
     * 
     * 【参数说明】
     * @param chNO 通道编号，标识要选中的通道
     *             - 取值范围：1-9（根据注释）
     *             - Ch1：通道1
     *             - Ch2：通道2
     *             - 其他通道根据设备支持而定
     *             - 注意：参数名为chNO（大写O），不是chNo（小写o）
     * 
     * 【返回值】
     * 无返回值（void）
     * 
     * 【调用时机】
     * 1. 外部代码需要选中特定通道时调用
     * 2. 工作模式切换时恢复选中状态
     * 3. 系统初始化时设置默认选中通道
     * 4. 用户通过其他方式（非点击）选择通道时调用
     * 
     * 【实现要求】
     * 1. 实现类应维护选中状态
     * 2. 设置选中后应取消之前选中通道的选中状态（互斥）
     * 3. 应触发选中状态变化事件（如有监听器）
     * 4. 应触发重绘操作以更新光标显示
     * 5. 通道不存在时应忽略该操作
     * 
     * 【选中状态管理】
     * - 选中状态：通道显示选中光标，可以移动和调整
     * - 未选中状态：通道不显示选中光标
     * - 互斥性：同一时间只有一个通道被选中
     * 
     * 【使用示例】
     * <pre>
     * IWaveManage waveManage = ...;
     * waveManage.setSelectCursor(1); // 选中通道1
     * // 此时通道1显示选中光标，其他通道取消选中
     * </pre>
     */
    public void setSelectCursor(int chNO);
    
    /**
     * 根据坐标选择通道光标
     * 
     * 【功能说明】
     * 根据用户点击的屏幕坐标，判断并选择对应的通道。
     * 该方法用于处理用户点击波形区域的交互操作。
     * 
     * 【参数说明】
     * @param x 点击位置的X坐标（像素）
     *          - 单位：像素
     *          - 坐标系：相对于波形显示区域左上角
     *          - 取值范围：0 到 波形显示区域宽度
     * @param y 点击位置的Y坐标（像素）
     *          - 单位：像素
     *          - 坐标系：相对于波形显示区域左上角
     *          - 取值范围：0 到 波形显示区域高度
     * 
     * 【返回值】
     * @return 选中的通道编号
     *         - 返回值范围：1-9，表示选中的通道编号
     *         - 返回0：表示没有选中任何通道（点击空白区域）
     *         - 返回-1：表示点击无效区域
     *         - 具体返回值取决于实现类的定义
     * 
     * 【调用时机】
     * 1. 用户点击波形显示区域时调用
     * 2. 用户触摸波形显示区域时调用
     * 3. 处理触摸事件时调用
     * 
     * 【实现要求】
     * 1. 实现类应根据坐标判断点击了哪个通道
     * 2. 应更新选中状态（互斥选择）
     * 3. 应触发选中状态变化事件（如有监听器）
     * 4. 应触发重绘操作以更新光标显示
     * 5. 应考虑通道的可见性，不可见通道不应被选中
     * 6. 应考虑通道的显示区域范围
     * 
     * 【坐标判断逻辑】
     * 1. 遍历所有可见通道
     * 2. 判断点击坐标是否在通道的显示区域内
     * 3. 如果在某个通道区域内，返回该通道编号
     * 4. 如果不在任何通道区域内，返回0或-1
     * 
     * 【使用示例】
     * <pre>
     * IWaveManage waveManage = ...;
     * // 用户点击坐标(100, 200)
     * int selectedCh = waveManage.selectCursor(100, 200);
     * if (selectedCh > 0) {
     *     // 选中了通道selectedCh
     * } else {
     *     // 没有选中任何通道
     * }
     * </pre>
     */
    public int selectCursor(int x, int y);
    
    /**
     * 移动当前选中通道的像素距离
     * 
     * 【功能说明】
     * 移动当前选中通道波形的垂直位置，移动距离以像素为单位。
     * 该方法用于处理用户拖动波形的交互操作。
     * 
     * 【参数说明】
     * @param px 移动的像素距离
     *           - 单位：像素
     *           - 正值：波形向下移动
     *           - 负值：波形向上移动
     *           - 0：不移动
     * 
     * 【返回值】
     * 无返回值（void）
     * 
     * 【调用时机】
     * 1. 用户拖动波形时调用（每次拖动事件）
     * 2. 系统自动调整波形位置时调用
     * 3. 外部代码需要微调波形位置时调用
     * 
     * 【实现要求】
     * 1. 实现类应获取当前选中通道
     * 2. 应将像素距离转换为位置比例值
     * 3. 应更新通道的位置状态
     * 4. 应触发波形移动事件（如有监听器）
     * 5. 应触发重绘操作以更新显示
     * 6. 应考虑位置的边界限制
     * 7. 如果没有选中通道，应忽略该操作
     * 
     * 【XY模式特殊说明】
     * - Ch1移动：水平移动（X轴方向）
     * - Ch2移动：垂直移动（Y轴方向）
     * 
     * 【像素到比例的转换】
     * - 位置比例 = 像素距离 / 显示区域高度
     * - 例如：移动50像素，显示区域高度500像素，则位置变化0.1
     * 
     * 【使用示例】
     * <pre>
     * IWaveManage waveManage = ...;
     * waveManage.setSelectCursor(1); // 先选中通道1
     * waveManage.movePix(50);  // 通道1向下移动50像素
     * waveManage.movePix(-30); // 通道1向上移动30像素
     * </pre>
     */
    public void movePix(int px);
    
    /**
     * 获取当前选中的通道编号
     * 
     * 【功能说明】
     * 获取当前处于选中状态的通道编号。
     * 用于查询当前哪个通道被用户选中，可以进行移动等操作。
     * 
     * 【参数说明】
     * 无参数
     * 
     * 【返回值】
     * @return 当前选中的通道编号
     *         - 返回值范围：1-9，表示选中的通道编号
     *         - 返回0：表示没有选中任何通道
     *         - 返回-1：表示选中状态无效
     *         - 具体返回值取决于实现类的定义
     * 
     * 【调用时机】
     * 1. 需要查询当前选中通道时调用
     * 2. 需要对选中通道进行操作前调用
     * 3. 显示选中状态信息时调用
     * 4. 保存选中状态到缓存时调用
     * 
     * 【实现要求】
     * 1. 实现类应维护选中状态
     * 2. 应返回当前选中的通道编号
     * 3. 如果没有选中通道，应返回0或-1
     * 4. 应保证返回值的有效性
     * 
     * 【使用场景】
     * - 查询当前选中通道，以便进行移动操作
     * - 显示当前选中通道的信息
     * - 保存选中状态以便恢复
     * - 工作模式切换时保存和恢复选中状态
     * 
     * 【使用示例】
     * <pre>
     * IWaveManage waveManage = ...;
     * int curCh = waveManage.getCurCh();
     * if (curCh > 0) {
     *     // 当前选中了通道curCh
     *     System.out.println("当前选中通道：" + curCh);
     * } else {
     *     // 没有选中任何通道
     *     System.out.println("没有选中通道");
     * }
     * </pre>
     */
    public int getCurCh();
}