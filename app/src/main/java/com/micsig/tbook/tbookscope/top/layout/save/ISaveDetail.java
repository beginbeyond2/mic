package com.micsig.tbook.tbookscope.top.layout.save; // 保存功能模块的包声明

/*
 * ╔══════════════════════════════════════════════════════════════════════╗
 * ║  模块定位: 保存功能模块 - 保存详情接口                                ║
 * ║  核心职责: 定义保存操作的详情契约接口，作为各保存类型的统一抽象          ║
 * ║  架构设计: 空接口（标记接口），用于类型约束和扩展点预留                  ║
 * ║  数据流向: 无直接数据流，作为类型标记被各保存实现类实现                  ║
 * ║  依赖关系: 无外部依赖，被 TopMsgSave 等系列类实现                      ║
 * ║  使用场景: 当需要对不同保存类型进行统一抽象时，作为类型约束接口          ║
 * ╚══════════════════════════════════════════════════════════════════════╝
 */

/**
 * Created by yangj on 2017/5/16.
 */

/**
 * 保存详情接口，定义保存操作的统一抽象契约
 * <p>作为标记接口使用，为各保存类型提供统一的类型约束</p>
 */
public interface ISaveDetail { // 保存详情标记接口，预留扩展点
}
