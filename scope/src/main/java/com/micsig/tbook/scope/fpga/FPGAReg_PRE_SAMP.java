package com.micsig.tbook.scope.fpga;                                                     // 包声明：FPGA模块

/**
 * ╔══════════════════════════════════════════════════════════════════════════════╗
 * ║        FPGAReg_PRE_SAMP - 示波器FPGA预采样寄存器类                             ║
 * ╠══════════════════════════════════════════════════════════════════════════════╣
 * ║ 【模块定位】                                                                 ║
 * ║   FPGA模块的预采样寄存器类，位于fpga包下，                                    ║
 * ║   继承自FPGAReg基类，负责管理FPGA预采样和后采样深度参数。                      ║
 * ║                                                                              ║
 * ║ 【核心职责】                                                                 ║
 * ║   1. 设置预采样深度（触发点前的数据量）                                       ║
 * ║   2. 设置后采样深度（触发点后的数据量）                                       ║
 * ║   3. 控制触发点在波形显示中的位置                                             ║
 * ║   4. 继承FPGAReg基类的寄存器操作功能                                          ║
 * ║                                                                              ║
 * ║ 【架构位置】                                                                 ║
 * ║   ┌──────────────────────────────────────────────────────────────────────┐ ║
 * ║   │                        FPGA寄存器架构                                  │ ║
 * ║   │                                                                      │ ║
 * ║   │   ┌─────────────────┐         ┌─────────────────┐                   │ ║
 * ║   │   │    FPGAReg      │────────▶│FPGAReg_PRE_SAMP │                   │ ║
 * ║   │   │   (寄存器基类)   │         │ (预采样寄存器)   │                   │ ║
 * ║   │   └─────────────────┘         └────────┬────────┘                   │ ║
 * ║   │          │                             │                            │ ║
 * ║   │          ▼                             ▼                            │ ║
 * ║   │   ┌─────────────────┐         ┌─────────────────┐                   │ ║
 * ║   │   │  寄存器操作方法  │         │  FPGACommand    │                   │ ║
 * ║   │   │  (setVal/getVal) │         │   (命令管理)     │                   │ ║
 * ║   │   └─────────────────┘         └─────────────────┘                   │ ║
 * ║   └──────────────────────────────────────────────────────────────────────┘ ║
 * ║                                                                              ║
 * ║ 【预采样概念说明】                                                           ║
 * ║   ┌──────────────────────────────────────────────────────────────────────┐ ║
 * ║   │                        预采样概念                                      │ ║
 * ║   │                                                                      │ ║
 * ║   │   【什么是预采样】                                                     │ ║
 * ║   │   - 预采样是指在触发点之前采集的数据量                                  │ ║
 * ║   │   - 允许查看触发事件发生之前的波形                                      │ ║
 * ║   │   - 预采样深度决定了触发前能看到的波形长度                              │ ║
 * ║   │                                                                      │ ║
 * ║   │   【什么是后采样】                                                     │ ║
 * ║   │   - 后采样是指在触发点之后采集的数据量                                  │ ║
 * ║   │   - 允许查看触发事件发生之后的波形                                      │ ║
 * ║   │   - 后采样深度决定了触发后能看到的波形长度                              │ ║
 * ║   │                                                                      │ ║
 * ║   │   【触发位置控制】                                                     │ ║
 * ║   │   - 预采样和后采样深度决定了触发点在屏幕上的位置                        │ ║
 * ║   │   - 预采样深度大：触发点在屏幕右侧（查看触发前波形）                    │ ║
 * ║   │   - 后采样深度大：触发点在屏幕左侧（查看触发后波形）                    │ ║
 * ║   │   - 预采样=后采样：触发点在屏幕中央                                    │ ║
 * ║   │                                                                      │ ║
 * ║   │   【应用场景】                                                         │ ║
 * ║   │   - 预触发分析：分析触发事件发生前的信号状态                            │ ║
 * ║   │   - 故障诊断：查看故障发生前的波形                                    │ ║
 * ║   │   - 信号分析：分析信号的完整变化过程                                  │ ║
 * ║   │   - 滚动模式：预采样深度设为0                                         │ ║
 * ║   └──────────────────────────────────────────────────────────────────────┘ ║
 * ║                                                                              ║
 * ║ 【寄存器格式】                                                               ║
 * ║   ┌──────────────────────────────────────────────────────────────────────┐ ║
 * ║   │                        寄存器数据格式                                  │ ║
 * ║   │                                                                      │ ║
 * ║   │   寄存器ID：FPGA_PRE_SAMP                                             │ ║
 * ║   │   数据长度：12字节（3个字，每个字32位）                                 │ ║
 * ║   │                                                                      │ ║
 * ║   │   【字索引分配】                                                       │ ║
 * ║   │   偏移量  │  位宽  │  参数名称    │  说明                              │ ║
 * ║   │   ───────┼───────┼─────────────┼─────────────────────────────────── │ ║
 * ║   │     0    │  32   │  preVol      │  预采样深度（触发点前数据量）       │ ║
 * ║   │     1    │  32   │  postVolL    │  后采样深度低位（触发点后数据量）   │ ║
 * ║   │     2    │  32   │  postVolH    │  后采样深度高位（扩展位）          │ ║
 * ║   │                                                                      │ ║
 * ║   │   【深度单位】                                                         │ ║
 * ║   │   - 单位：采样点数                                                    │ ║
 * ║   │   - 预采样深度：触发点前的采样点数量                                  │ ║
 * ║   │   - 后采样深度：触发点后的采样点数量                                  │ ║
 * ║   │   - 总采样深度 = 预采样深度 + 后采样深度                              │ ║
 * ║   │                                                                      │ ║
 * ║   │   【深度范围】                                                         │ ║
 * ║   │   - 预采样深度：0到存储深度                                           │ ║
 * ║   │   - 后采样深度：0到存储深度                                           │ ║
 * ║   │   - 滚动模式：预采样深度为0                                           │ ║
 * ║   │   - 正常模式：预采样深度根据触发位置计算                              │ ║
 * ║   └──────────────────────────────────────────────────────────────────────┘ ║
 * ║                                                                              ║
 * ║ 【使用流程】                                                                 ║
 * ║   ┌──────────────────────────────────────────────────────────────────────┐ ║
 * ║   │                        寄存器使用流程                                  │ ║
 * ║   │                                                                      │ ║
 * ║   │   设置触发位置                                                        │ ║
 * ║   │         │                                                             │ ║
 * ║   │         ▼                                                             │ ║
 * ║   │   计算预采样和后采样深度                                              │ ║
 * ║   │   根据触发位置和存储深度计算                                          │ ║
 * ║   │         │                                                             │ ║
 * ║   │         ▼                                                             │ ║
 * ║   │   创建预采样寄存器对象                                                │ ║
 * ║   │   FPGAReg_PRE_SAMP reg = new FPGAReg_PRE_SAMP()                      │ ║
 * ║   │         │                                                             │ ║
 * ║   │         ▼                                                             │ ║
 * ║   │   设置预采样深度                                                      │ ║
 * ║   │   reg.setPreVol(preDepth)                                            │ ║
 * ║   │         │                                                             │ ║
 * ║   │         ▼                                                             │ ║
 * ║   │   设置后采样深度                                                      │ ║
 * ║   │   reg.setPostVolL(postDepthL)                                        │ ║
 * ║   │   reg.setPostVolH(postDepthH)                                        │ ║
 * ║   │         │                                                             │ ║
 * ║   │         ▼                                                             │ ║
 * ║   │   发送命令到FPGA                                                      │ ║
 * ║   │   sendCommand(reg)                                                   │ ║
 * ║   │         │                                                             │ ║
 * ║   │         ▼                                                             │ ║
 * ║   │   FPGA配置预采样参数                                                  │ ║
 * ║   │         │                                                             │ ║
 * ║   │         ▼                                                             │ ║
 * ║   │   触发位置生效                                                        │ ║
 * ║   └──────────────────────────────────────────────────────────────────────┘ ║
 * ║                                                                              ║
 * ║ 【设计模式】                                                                 ║
 * ║   - 继承模式：继承FPGAReg基类，复用寄存器操作功能                           ║
 * ║                                                                              ║
 * ║ 【依赖关系】                                                                 ║
 * ║   - FPGAReg: FPGA寄存器基类                                               ║
 * ║   - FPGACommand: FPGA命令管理类，负责发送命令                              ║
 * ║   - Trigger: 触发管理类，管理触发位置                                      ║
 * ║   - Sample: 采样管理类，管理存储深度                                      │ ║
 * ║                                                                              ║
 * ║ 【应用场景】                                                                 ║
 * ║   1. 触发位置调整：调整触发点在屏幕上的位置                                ║
 * ║   2. 预触发查看：查看触发事件发生前的波形                                  ║
 * ║   3. 后触发查看：查看触发事件发生后的波形                                  ║
 * ║   4. 滚动模式：预采样深度设为0                                            ║
 * ║   5. 波形分析：分析信号的完整变化过程                                     ║
 * ║                                                                              ║
 * ║ 【作者】 MHO项目组                                                           ║
 * ║ 【创建日期】 2018-4-8                                                        ║
 * ╚══════════════════════════════════════════════════════════════════════════════╝
 */

/**
 * 示波器FPGA预采样寄存器类
 * 继承自FPGAReg基类，负责管理FPGA预采样和后采样深度参数
 *
 * <p><b>主要功能：</b></p>
 * <ul>
 *   <li>设置预采样深度（触发点前的数据量）</li>
 *   <li>设置后采样深度（触发点后的数据量）</li>
 *   <li>控制触发点在波形显示中的位置</li>
 * </ul>
 *
 * <p><b>使用示例：</b></p>
 * <pre>
 * // 创建预采样寄存器对象
 * FPGAReg_PRE_SAMP reg = new FPGAReg_PRE_SAMP();
 *
 * // 设置预采样深度（触发点居中，预采样50%）
 * int memDepth = 10000;  // 存储深度
 * int preDepth = memDepth / 2;  // 预采样深度
 * int postDepth = memDepth / 2;  // 后采样深度
 *
 * reg.setPreVol(preDepth);  // 设置预采样深度
 * reg.setPostVolL(postDepth);  // 设置后采样深度低位
 * reg.setPostVolH(0);  // 设置后采样深度高位
 *
 * // 发送命令到FPGA
 * FPGACommand.getInstance().sendCommand(reg);
 *
 * // 滚动模式下预采样深度设为0
 * reg.setPreVol(0);  // 滚动模式
 * </pre>
 *
 * @see FPGAReg
 * @see FPGACommand
 * @see Trigger
 * @see Sample
 * @author zhuzh
 * @version 1.0
 * @since 2018-4-8
 */
public class FPGAReg_PRE_SAMP extends FPGAReg {                                           // 类声明：预采样寄存器类，继承FPGAReg

    // ═══════════════════════════════════════════════════════════════════════════════
    // 构造方法
    // ═══════════════════════════════════════════════════════════════════════════════

    /**
     * 构造函数
     * 初始化预采样寄存器
     *
     * <p><b>初始化参数：</b></p>
     * <ul>
     *   <li>寄存器ID：FPGA_PRE_SAMP</li>
     *   <li>数据长度：12字节（3个字，每个字32位）</li>
     * </ul>
     *
     * <p><b>寄存器地址说明：</b></p>
     * <pre>
     * ┌─────────────────────────────────────────┐
     * │  寄存器地址映射                         │
     * ├─────────────────────────────────────────┤
     * │  FPGA_PRE_SAMP (0x04)                   │
     * │  功能: 设置预采样和后采样深度           │
     * │  大小: 12字节（3个字）                  │
     * │  访问: 只写 (由FPGA读取)                │
     * │                                         │
     * │  数据结构:                              │
     * │  字0: 预采样深度（32位）                │
     * │  字1: 后采样深度低位（32位）            │
     * │  字2: 后采样深度高位（32位）            │
     * └─────────────────────────────────────────┘
     * </pre>
     *
     * <p><b>调用时机：</b></p>
     * <ul>
     *   <li>触发位置改变时，需要重新计算预采样和后采样深度</li>
     *   <li>存储深度改变时，需要重新设置预采样参数</li>
     *   <li>进入或退出滚动模式时，需要调整预采样深度</li>
     * </ul>
     */
    public FPGAReg_PRE_SAMP() {                                                           // 构造方法：初始化预采样寄存器
        // 调用父类构造函数，设置寄存器ID和数据长度
        // FPGA_PRE_SAMP：预采样寄存器地址
        // 12：数据长度为12字节（3个32位字）
        super(FPGA_PRE_SAMP, 12);                                                          // 调用父类构造函数，设置寄存器ID为FPGA_PRE_SAMP，长度12字节
    }                                                                                     // 构造方法结束

    // ═══════════════════════════════════════════════════════════════════════════════
    // 公有方法 - 预采样深度设置
    // ═══════════════════════════════════════════════════════════════════════════════

    /**
     * 设置预采样深度
     * 设置触发点之前的数据采集量
     *
     * <p><b>功能说明：</b></p>
     * <ul>
     *   <li>预采样深度决定了触发点前能看到的波形长度</li>
     *   <li>预采样深度越大，触发点越靠右（查看更多触发前波形）</li>
     *   <li>滚动模式下预采样深度设为0</li>
     *   <li>正常模式下预采样深度根据触发位置计算</li>
     * </ul>
     *
     * <p><b>参数范围：</b></p>
     * <ul>
     *   <li>最小值：0（触发点在最左侧，不显示触发前波形）</li>
     *   <li>最大值：存储深度（触发点在最右侧，只显示触发前波形）</li>
     *   <li>典型值：存储深度的一半（触发点居中）</li>
     * </ul>
     *
     * <p><b>数据存储位置：</b></p>
     * <pre>
     * ┌─────────────────────────────────────────┐
     * │  寄存器位映射                           │
     * ├─────────────────────────────────────────┤
     * │  字0: preVol (32位)                     │
     * │                                         │
     * │  示例: val = 5000                       │
     * │    存储位置: 字0（偏移量0）             │
     * │    表示触发点前采集5000个采样点         │
     * └─────────────────────────────────────────┘
     * </pre>
     *
     * <p><b>应用场景：</b></p>
     * <ul>
     *   <li>触发位置调整：改变触发点在屏幕上的位置</li>
     *   <li>预触发分析：查看触发事件发生前的信号状态</li>
     *   <li>滚动模式：预采样深度设为0</li>
     * </ul>
     *
     * @param val 预采样深度值（采样点数）
     */
    public void setPreVol(int val){                                                       // 公有方法：设置预采样深度
        // 调用父类setVal方法，设置字0的值
        // 参数1：字索引=0（第一个32位字）
        // 参数2：预采样深度值
        setVal(0, val);                                                                   // 设置字0的值，值为val（预采样深度）
    }                                                                                     // 方法结束

    // ═══════════════════════════════════════════════════════════════════════════════
    // 公有方法 - 后采样深度设置
    // ═══════════════════════════════════════════════════════════════════════════════

    /**
     * 设置后采样深度低位
     * 设置触发点之后的数据采集量（低位32位）
     *
     * <p><b>功能说明：</b></p>
     * <ul>
     *   <li>后采样深度决定了触发点后能看到的波形长度</li>
     *   <li>后采样深度越大，触发点越靠左（查看更多触发后波形）</li>
     *   <li>后采样深度使用64位存储，分为低位和高位两部分</li>
     *   <li>低位存储后采样深度的低32位</li>
     * </ul>
     *
     * <p><b>参数范围：</b></p>
     * <ul>
     *   <li>最小值：0（触发点在最右侧，不显示触发后波形）</li>
     *   <li>最大值：存储深度（触发点在最左侧，只显示触发后波形）</li>
     *   <li>典型值：存储深度的一半（触发点居中）</li>
     * </ul>
     *
     * <p><b>数据存储位置：</b></p>
     * <pre>
     * ┌─────────────────────────────────────────┐
     * │  寄存器位映射                           │
     * ├─────────────────────────────────────────┤
     * │  字1: postVolL (32位)                   │
     * │                                         │
     * │  示例: val = 5000                       │
     * │    存储位置: 字1（偏移量1）             │
     * │    表示触发点后采集5000个采样点         │
     * │    （低位部分）                         │
     * └─────────────────────────────────────────┘
     * </pre>
     *
     * <p><b>应用场景：</b></p>
     * <ul>
     *   <li>触发位置调整：改变触发点在屏幕上的位置</li>
     *   <li>后触发分析：查看触发事件发生后的信号状态</li>
     *   <li>大存储深度：需要同时设置高位和低位</li>
     * </ul>
     *
     * @param val 后采样深度低位值（采样点数）
     */
    public void setPostVolL(int val){                                                     // 公有方法：设置后采样深度低位
        // 调用父类setVal方法，设置字1的值
        // 参数1：字索引=1（第二个32位字）
        // 参数2：后采样深度低位值
        setVal(1, val);                                                                   // 设置字1的值，值为val（后采样深度低位）
    }                                                                                     // 方法结束

    /**
     * 设置后采样深度高位
     * 设置触发点之后的数据采集量（高位32位）
     *
     * <p><b>功能说明：</b></p>
     * <ul>
     *   <li>后采样深度使用64位存储，分为低位和高位两部分</li>
     *   <li>高位存储后采样深度的高32位</li>
     *   <li>用于支持超大存储深度（超过32位范围）</li>
     *   <li>通常情况下高位值为0</li>
     * </ul>
     *
     * <p><b>参数范围：</b></p>
     * <ul>
     *   <li>最小值：0</li>
     *   <li>最大值：根据存储深度确定</li>
     *   <li>典型值：0（存储深度不超过32位范围）</li>
     * </ul>
     *
     * <p><b>数据存储位置：</b></p>
     * <pre>
     * ┌─────────────────────────────────────────┐
     * │  寄存器位映射                           │
     * ├─────────────────────────────────────────┤
     * │  字2: postVolH (32位)                   │
     * │                                         │
     * │  示例: val = 0                          │
     * │    存储位置: 字2（偏移量2）             │
     * │    表示后采样深度高位为0                │
     * │    （存储深度不超过32位）               │
     * └─────────────────────────────────────────┘
     * </pre>
     *
     * <p><b>应用场景：</b></p>
     * <ul>
     *   <li>超大存储深度：存储深度超过32位范围时使用</li>
     *   <li>高精度采样：需要更大的采样深度时使用</li>
     * </ul>
     *
     * @param val 后采样深度高位值（采样点数）
     */
    public void setPostVolH(int val){                                                     // 公有方法：设置后采样深度高位
        // 调用父类setVal方法，设置字2的值
        // 参数1：字索引=2（第三个32位字）
        // 参数2：后采样深度高位值
        setVal(2, val);                                                                   // 设置字2的值，值为val（后采样深度高位）
    }                                                                                     // 方法结束

    // ═══════════════════════════════════════════════════════════════════════════════
    // 公有方法 - 数据读取
    // ═══════════════════════════════════════════════════════════════════════════════

    /**
     * 获取预采样深度
     * 返回当前设置的预采样深度值
     *
     * <p><b>功能说明：</b></p>
     * <ul>
     *   <li>读取寄存器中存储的预采样深度值</li>
     *   <li>返回字0的值（预采样深度）</li>
     *   <li>用于验证设置是否正确</li>
     * </ul>
     *
     * <p><b>返回值说明：</b></p>
     * <ul>
     *   <li>返回预采样深度值（采样点数）</li>
     *   <li>值范围：0到存储深度</li>
     * </ul>
     *
     * <p><b>应用场景：</b></p>
     * <ul>
     *   <li>验证设置：确认预采样深度设置是否正确</li>
     *   <li>状态查询：查询当前的预采样深度</li>
     * </ul>
     *
     * @return 预采样深度值（采样点数）
     */
    public int getPreVol(){                                                               // 公有方法：获取预采样深度
        // 调用父类getVal方法，获取字0的值
        // 返回预采样深度值
        return getVal();                                                                  // 返回字0的值（预采样深度）
    }                                                                                     // 方法结束

}                                                                                         // 类结束